 {{!-- <div class="container jumbotron">
      <h3>Participant Information
          <a href="/member/{{id}}/update" class="btn btn-dark btn-sm mr-2" style="color: white"><i class="fas fa-edit"></i></a>
            <a href="/member/{{id}}/delete" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this participant?')"><i class="fas fa-trash-alt"></i></a>
      </h3>
      <hr class="divider my-4">
      <div>
        <div id="noColor"></div>
        <div class="tableDiv"></div>
            <div class="row mb-2">
                <label class="col-lg-auto font-weight-bold mb-0">Name:</label>
                <span class="col-lg-auto">{{Name}}</span>
            </div>
            <div class="row mb-2">
                <label class="col-lg-auto col-sm-12 text-left font-weight-bold mb-0">Team Name:</label>
                <span class="col-lg-auto col-sm-12">{{TeamName}}</span>
            </div>
            <div class="row mb-2">
                <label class="col-lg-auto col-sm-12 text-left font-weight-bold mb-0">Email Address:</label>
                <span class="col-lg-auto col-sm-12">{{EmailAddress}}</span>
            </div>
            <div class="row mb-2">
                <label class="col-lg-auto col-sm-12 text-left font-weight-bold mb-0">Phone Number:</label>
                <span class="col-lg-auto col-sm-12">{{PhoneNumber}}</span>
            </div>
            <div class="row">
                <label class="col-lg-auto col-sm-12 text-left font-weight-bold mb-0">Amount:</label>
                <span class="col-lg-auto col-sm-12">${{Amount}}</span>
            </div>
      </div>
    </div> --}}

<section class="page-section" id="viewEntry">
    <div class="container jumbotron">
      <div class="row">
        <div class="col-lg-8 text-center">
                <h3>Participant Information
          <a href="/member/{{id}}/update" class="btn btn-dark btn-sm mr-2" style="color: white"><i class="fas fa-edit"></i></a>
            <a href="/member/{{id}}/delete" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this participant?')"><i class="fas fa-trash-alt"></i></a>
      </h3>
          <hr class="divider my-4">
            <div class="row mb-2">
                <label class="col-lg-auto font-weight-bold mb-0">Name:</label>
                <span class="col-lg-auto">{{Name}}</span>
            </div>
            <div class="row mb-2">
                <label class="col-lg-auto col-sm-12 font-weight-bold mb-0">Team Name:</label>
                <span class="col-lg-auto col-sm-12">{{TeamName}}</span>
            </div>
            <div class="row mb-2">
                <label class="col-lg-auto col-sm-12 font-weight-bold mb-0">Email Address:</label>
                <span class="col-lg-auto col-sm-12">{{EmailAddress}}</span>
            </div>
            <div class="row mb-2">
                <label class="col-lg-auto col-sm-12 font-weight-bold mb-0">Phone Number:</label>
                <span class="col-lg-auto col-sm-12">{{PhoneNumber}}</span>
            </div>
            <div class="row">
                <label class="col-lg-auto col-sm-12 font-weight-bold mb-0">Amount:</label>
                <span class="col-lg-auto col-sm-12">${{Amount}}</span>
            </div>
        </div>
      </div>

    </div>
  </section>